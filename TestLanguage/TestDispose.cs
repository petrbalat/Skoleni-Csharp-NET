using System.IO;
using NUnit.Framework;

namespace TestLanguage
{
    [TestFixture]
    public class TestDispose
    {
        [Test]
        public void Test()
        {
            FileStream fileStream = File.OpenRead("");
            StringReader reader = new Strin();
        }
    }
}